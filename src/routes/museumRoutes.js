/* const express = require("express");
const { isAuthenticated } = require("../middleware/authenticationMiddleware");
const router = express.Router();
const {
  getAllMuseums,
  getMuseumById,
  createMuseum,
  deleteMuseumById,
} = require("../controllers/museumControllers");
const { userRoles } = require("../constants/users");

router.get("/museums", isAuthenticated, getAllMuseums);
// router.get("/museums/:museumId", isAuthenticated, getMuseumById);
// router.post("/museum", isAuthenticated, createMuseum)
// router.delete("/museum/:museumId", isAuthenticated, deleteMuseumById)
 */
